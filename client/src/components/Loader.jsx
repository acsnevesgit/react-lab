import CircularProgress from '@mui/material/CircularProgress';

const Loader = () => {

  // ----- Render -----

  return (
    <div className="loader">
      <CircularProgress />
    </div>
  );
};

export default Loader;